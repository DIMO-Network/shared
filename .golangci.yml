run:
  timeout: 5m
linters:
  enable:
    - prealloc
    - revive
    - goimports
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - zerologlint

issues:
  fix: true
